[package]
name = "zenb-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
zenb-store = { path = "../zenb-store" }
zenb-core = { path = "../zenb-core" }
hex = "0.4"
